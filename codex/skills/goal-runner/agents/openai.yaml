interface:
  display_name: "Goal Runner"
  short_description: "Run a ~/.tasks spec to external review."
  default_prompt: "Use $goal-runner with a task folder or task key to implement spec.md and leave review-packet.md for Claude Code review."
